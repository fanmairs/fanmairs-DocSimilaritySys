以准确率P、召回率R、mAP@0.5这种优越性能得益于MSPA机制独特的特征优化能力，其通过有效降低特征图间的冗余信息，显著提升了模型的特征表达能力。相比之下，CA机制表现次优，背景复杂等特点，本研究以YOLOv11模型为基准模型，提出了一种针对于肺结节图像检测算法SKM-YOLO。在LUNA16数据集以及LIDC-IDRI数据集上实最终，通过频域建模、胶囊感知以及RF-APM的多模块协同，Caps-FDRNet在语义信息传导与空间细节还原之间实现了高效融合，显著提升了肺结节CT图操作，这可能在处理如肺结节这样具有复杂结构的目标时存在一定的局限性。

针对于这一问题，本文在U型网络的下采样卷积层引入频域动态卷积FDConv[74]，权重重组：将空间域中转换结果裁剪为kk的局部卷积块，并按照标准卷积核的权重形状进行重组。最终生成的卷积核权重形状为kkCinCout。KSM不再使用稀疏的向量进行权重融合，而是预测一个密集的调制矩阵inCout用于逐位置、逐通道地调制每一个卷积核的空间响应。RkkC部通道分支采用了一种高效的轻量级一维卷积，该分支能够提取通道内的局部依赖关系，并预测大小为kkCinCout的密集调制矩阵，该结构在显著降低模型参数量与计算复杂度的同时，依然保持了对卷积核中各元素进行细粒度调制其中X,Yhw为输入与输出的特征图，Abhw表示第b个频带的空间调制矩阵，Wb为第b个频带的权重分量。

Wb的获取如式(4.2)所示，其中和1分别为DFT和iDFT，Mb为二值频带掩膜。该机制能够在不同空间位置对不同具体而言，如图4.4所示，胶囊层接收的输入特征图维度为HWCA，其中HW分别表示空间尺度，C为胶囊的类别数，A是每一类胶囊的向量维度。随后，通过将所有Ui按照动态系数加权汇总，形成胶囊层的输入向量S。最终，引入压缩函数squash对S进行非线性变换，一方面保持向量方向不变，其中i为胶囊类别，u为上层胶囊的输入，W为变换矩阵，ˆiu表示高层胶囊的输入，ic是在动态路由更新中计算得到的权重系数。该算法通过多轮迭代过程不断调整ic的取值，迭代次数记为超参数r，在本架构中设定为3。线性映射是由卷积层实现的，其中W实际上代表了卷积核的参数。
